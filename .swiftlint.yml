disabled_rules:
  - line_length
  - trailing_whitespace
  - type_body_length
  - file_length
  - function_body_length

opt_in_rules:
  - empty_count
  - closure_spacing

excluded:
  - build
  - DerivedData
  - Pods
